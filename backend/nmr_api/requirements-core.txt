# RuuPhenome — core runtime (torch-free).
# This is everything the live demo needs: Track 1 profiling, Track 2 biomarkers,
# biology, and the web UI. PyTorch is intentionally EXCLUDED — it is only used by
# the optional self-supervised encoder / NMRformer, which are off by default and
# would add ~2 GB to the image (too heavy for the 8 GB competition VM).
#
# To enable the optional deep-learning extras, also install requirements.txt.

fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pandas>=2.0
numpy>=1.24
pydantic>=2.0
requests>=2.28

# Domain 1 — signal processing
nmrglue>=0.10
scipy>=1.10

# Domain 2 — biomarker discovery
scikit-learn>=1.3
xgboost>=3.0,<4
umap-learn>=0.5,<0.6
