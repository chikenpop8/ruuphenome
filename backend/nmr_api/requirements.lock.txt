# RuuPhenome — exact dependency pins for reproducibility.
# Reproduces the reported honest_roc_auc / Q² / CI numbers bit-for-bit on a clean host.
# Verified environment: Python 3.13.2 (see .python-version).
#   pip install -r requirements.lock.txt
# `requirements.txt` keeps human-readable minimum bounds for dev flexibility;
# this lockfile is the authoritative pinned set (Sandve 2013 "Ten Simple Rules
# for Reproducible Computational Research", rule 3: archive exact versions).
fastapi==0.138.0
uvicorn==0.49.0
python-multipart==0.0.32
pandas==3.0.3
numpy==2.4.6
pydantic==2.13.4
requests==2.34.2
nmrglue==0.11
scipy==1.18.0
scikit-learn==1.9.0
xgboost==3.3.0
umap-learn==0.5.12
torch==2.12.1
